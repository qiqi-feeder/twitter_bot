# Web3 每日大盘复盘 - 使用说明

## 🎯 核心功能

自动获取市场数据 → 使用自定义提示词生成复盘 → 发布到 Twitter

---

## 📁 重要文件

| 文件 | 说明 | 操作 |
|------|------|------|
| `prompt/system_prompt.md` | **系统提示词**（定义复盘风格和结构） | ✅ 可修改 |
| `config/config.yaml` | 系统配置（API Key、定时任务） | ✅ 可修改 |
| `data/market.json` | 最新市场数据缓存 | 自动生成 |
| `data/recap_output.json` | 最新生成的复盘内容 | 自动生成 |

---

## 🚀 快速开始

### 1️⃣ 测试完整流程（不发布）

```bash
python tools/test_recap_with_prompt.py
```

**这个命令会：**
- ✅ 获取真实市场数据（BTC/ETH 价格、市值、恐惧贪婪指数等）
- ✅ 使用 `prompt/system_prompt.md` 作为系统提示词
- ✅ 调用 LLM 生成复盘 Thread
- ✅ 显示生成的每条推文（不发布）
- ✅ 保存结果到 `data/recap_output.json`

**预期输出示例：**
```
🚀 开始测试大盘复盘功能（使用 prompt 文件夹）

============================================================
步骤 1/3: 获取市场数据
============================================================
✅ 市场数据获取成功
  BTC: $91737.05 (+0.13%)
  ETH: $3001.25 (-3.25%)
  总市值: $3,214,876,752,513
  恐惧贪婪指数: 11 (Extreme Fear)

============================================================
步骤 2/3: 使用 LLM 生成复盘内容
============================================================
✅ LLM 生成成功

============================================================
步骤 3/3: 生成的复盘 Thread
============================================================
共生成 5 条推文:

Tweet 1/5 (长度: 245 字符)
============================================================
📊 11月20日 Web3大盘复盘 | BTC 微涨守住 $91K，山寨币继续承压 🩸
...
```

---

### 2️⃣ 自定义系统提示词

编辑 `prompt/system_prompt.md` 文件，修改复盘的风格和结构。

**当前提示词特点：**
- 角色：专业 Web3 市场分析师 + 推特 KOL
- 风格：观点犀利、数据驱动、拒绝废话
- 结构：8 个板块（标题、总览、宏观、核心数据、重点币种、链上数据、重磅新闻、明日关注）

**修改示例：**

如果你想要更简洁的复盘，可以修改为：

```markdown
## Role
你是一名专业的加密货币市场分析师，风格简洁明了。

## Goal
生成 3-4 条推文的每日复盘 Thread。

## Output Structure

**Tweet 1: 核心数据**
- BTC/ETH 价格和涨跌
- 总市值和情绪指数

**Tweet 2: 市场分析**
- 价格走势分析
- 交易量变化

**Tweet 3: 总结**
- 一句话总结
- 明日关注
```

保存后，再次运行测试脚本即可看到效果。

---

### 3️⃣ 启用定时自动发布

**步骤 1：** 编辑 `config/config.yaml`

```yaml
scheduler:
  # 时区设置
  timezone: "Asia/Shanghai"  # 中国时间
  
  # 每日复盘配置
  daily_recap:
    enabled: true        # 启用自动复盘
    recap_time: "20:00"  # 每天晚上 8 点
```

**步骤 2：** 启动系统

```bash
python app.py
```

系统会在每天 20:00 自动执行：获取数据 → 生成复盘 → 发布到 Twitter

---

### 4️⃣ 手动触发发布

```bash
# 方式 1: 使用 curl
curl -X POST http://localhost:5000/recap/manual

# 方式 2: 使用 Python
python -c "import requests; print(requests.post('http://localhost:5000/recap/manual').json())"
```

---

## 🔧 工作原理

```
┌─────────────────┐
│  定时/手动触发   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  1. 获取数据    │ ← Binance, CoinGecko, alternative.me
│  → market.json  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  2. 调用 LLM    │
│  系统提示词:     │ ← prompt/system_prompt.md
│  用户提示词:     │ ← market.json 数据
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  3. 生成 Thread │
│  → recap_output │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  4. 发布推特    │ → Twitter API
└─────────────────┘
```

---

## 📚 详细文档

- **[快速开始指南](docs/QUICK_START.md)** - 5 分钟上手教程
- **[完整使用指南](docs/RECAP_USAGE_GUIDE.md)** - 详细功能说明、配置、故障排查

---

## ❓ 常见问题

### Q: 如何修改复盘的风格？
**A:** 编辑 `prompt/system_prompt.md` 文件，修改 `Role` 和 `Constraints & Tone` 部分。

### Q: 如何修改复盘的内容结构？
**A:** 编辑 `prompt/system_prompt.md` 文件，修改 `Output Structure` 部分。

### Q: 如何更换 LLM 模型？
**A:** 编辑 `config/config.yaml`，修改 `openai.model` 字段为 `gpt-4` 或 `gpt-3.5-turbo`。

### Q: 测试时不想发布到 Twitter 怎么办？
**A:** 使用测试脚本 `python tools/test_recap_with_prompt.py`，只会生成内容不会发布。

### Q: 如何修改定时发布时间？
**A:** 编辑 `config/config.yaml`，修改 `scheduler.daily_recap.recap_time` 字段。

---

## 🎉 开始使用

现在就运行测试脚本试试吧：

```bash
python tools/test_recap_with_prompt.py
```

祝你使用愉快！🚀

